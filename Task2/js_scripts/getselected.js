/*function getSelectedRows() {
    var checkboxes = document.getElementById("checkbox");
    var selectedRows = [];
    for (var i = 0; i < checkboxes.length; i++) {
        if (checkboxes[i].checked) {
            var row = checkboxes[i].parentElement.parentElement;
            var title = row.cells[1].innerHTML;
            var overview = row.cells[2].innerHTML;
            var details = row.cells[3].innerHTML;
            var fees = row.cells[4].innerHTML;
            selectedRows.push([title, overview, details, fees]);
        }
    }
    return selectedRows;
}

$(document).ready(function() {
    $("#btnGenerate").click(function(e) {
      e.preventDefault();
      
    });
  
});*/